# Snowflake 運用設計書

| 項目 | 内容 |
|---|---|
| 文書名 | Snowflake データ基盤 運用設計書 |
| 版数 | 1.0(ドラフト) |
| 作成日 | 2026-07-10 |
| 作成者 | データ活用推進 |
| 承認者 | (記入) |
| 対象システム | Snowflake データ基盤(Hub & Spoke 構成) |

---

## 1. 本書の目的と位置づけ

### 1.1 目的
本書は、Snowflake を中核とするデータ基盤の運用に関する方針・体制・手順の基準を定め、以下を実現することを目的とする。

- データ基盤の安定稼働と、障害発生時の迅速な検知・復旧
- 金融規制(FISC 安全対策基準等)を踏まえた統制の維持
- コストの可視化と適正化
- 開発〜本番リリースまでの変更管理の統制

### 1.2 適用範囲
- Snowflake アカウント全体(Hub アカウントおよび各 Spoke)
- 周辺連携:データソース(Bloomberg / OMS / JASDEC 等)、BI(Tableau)、IaC / CI/CD(Terraform / schemachange / dbt)、認証基盤(Entra ID)
- 対象外:各業務システム側の運用(連携 I/F の Snowflake 側受け口までを対象とする)

### 1.3 関連文書
- Snowflake ガバナンス設計書(RBAC・Future Grants 設計)
- データライフサイクル管理基準
- リリース管理手順書 / CI/CD 設計書
- 障害対応手順書(インシデント管理規程)

---

## 2. システム構成概要

### 2.1 論理構成
- **アーキテクチャ**: Hub & Spoke モデル
  - Hub:全社共通データ(参照マスタ、共通ディメンション)、ガバナンス統制の中心
  - Spoke:部門・ユースケース単位のワークスペース(運用部、リスク管理部、コンプライアンス等)
- **データレイヤ**: メダリオンアーキテクチャ
  - RAW(Bronze):ソースそのままの取込層
  - STAGING / REFINED(Silver):クレンジング・正規化層
  - MART(Gold):業務利用・BI 参照層

### 2.2 環境構成

| 環境 | 用途 | データ | 変更方法 |
|---|---|---|---|
| DEV | 開発・単体検証 | マスク済み/サンプル | 開発者による直接変更可(スキーマ統制ルールに従う) |
| STG | 結合検証・リリース検証 | 本番同等(マスク適用) | CI/CD 経由のみ |
| PROD | 本番 | 本番データ | CI/CD 経由のみ。手動変更は緊急時手順に限る |

### 2.3 認証・接続方式

| 主体 | 認証方式 | 備考 |
|---|---|---|
| 一般ユーザー | Entra ID SSO(SAML)+ OAuth | パスワード認証は原則禁止 |
| Tableau | OAuth(Entra ID) | 接続エラー時の切り分け手順は §7.4 |
| サービスアカウント(ETL / dbt / schemachange) | Key Pair 認証 | 鍵ローテーションは §5.4 |
| 外部ベンダー | 委任ロール経由の限定アクセス | ネットワークポリシーで接続元制限 |

---

## 3. 運用体制と役割分担

### 3.1 体制

| ロール | 担当 | 主な責務 |
|---|---|---|
| データ基盤オーナー | データ活用推進 | 全体方針、コスト・統制の最終判断 |
| プラットフォーム管理者 | データ活用推進(運用担当) | アカウント設定、RBAC、監視、パッチ対応 |
| Spoke 管理者 | 各部門のデータ担当 | Spoke 内のオブジェクト管理、利用申請の一次承認 |
| 開発ベンダー | 外部委託先 | 委任された範囲での開発・保守作業 |
| 情報セキュリティ | 情報システム部門 | セキュリティ監査、インシデント時の統制 |

### 3.2 権限設計の原則(要点)
- ACCOUNTADMIN の利用は緊急時・初期設定時に限定し、利用時は事前申請と操作記録を必須とする
- 機能ロール(FR_)とアクセスロール(AR_)を分離し、ユーザーには機能ロールのみ付与する
- Future Grants はデータベースレベルで設定し、スキーマレベルの上書き挙動に注意する(詳細はガバナンス設計書参照)
- ベンダーへの権限は委任専用ロールに限定し、本番データへの直接アクセスは付与しない

---

## 4. 監視・アラート設計

### 4.1 監視項目

| 分類 | 監視項目 | 情報源 | 閾値・条件(例) | 通知先 |
|---|---|---|---|---|
| 稼働 | タスク/パイプ失敗 | TASK_HISTORY / COPY_HISTORY | 失敗検知即時 | 運用担当(Teams/メール) |
| 性能 | 長時間クエリ | QUERY_HISTORY | 実行 30 分超 | 運用担当 |
| 性能 | ウェアハウスのキュー滞留 | WAREHOUSE_LOAD_HISTORY | キュー待ち恒常化 | 運用担当 |
| コスト | クレジット消費 | METERING_HISTORY / Resource Monitor | 月次予算の 70% / 90% / 100% | 基盤オーナー |
| セキュリティ | ログイン失敗・不審アクセス | LOGIN_HISTORY | 連続失敗、未知の接続元 | 情シス・運用担当 |
| セキュリティ | 権限変更 | GRANTS_TO_* / ACCESS_HISTORY | ACCOUNTADMIN 操作、GRANT 実行 | 情シス(週次レビュー) |
| データ品質 | 取込件数の異常 | ロードログ / dbt test | 前日比 ±閾値超過、テスト失敗 | データ担当 |

> ACCOUNT_USAGE ビューには最大 45 分〜3 時間の遅延があるため、即時性が必要な監視は INFORMATION_SCHEMA のテーブル関数(直近履歴)を併用する。

### 4.2 Resource Monitor 設定方針
- アカウントレベルとウェアハウスレベルの二段構えで設定する
- 到達アクションは「70%: 通知」「90%: 通知」「100%: SUSPEND(本番 ETL 用 WH は通知のみとし業務影響を回避)」を基本とする
- 予算は四半期ごとに見直す

### 4.3 定期レビュー

| 頻度 | 内容 |
|---|---|
| 日次 | ジョブ実行結果、失敗有無、前日クレジット消費 |
| 週次 | 権限変更ログ、長時間クエリ傾向、ストレージ増加 |
| 月次 | コストレポート(部門別チャージバック)、未使用オブジェクト・休眠ユーザーの棚卸し |
| 四半期 | RBAC 棚卸し(全ロール・付与の妥当性確認)、ネットワークポリシー見直し |

---

## 5. セキュリティ運用

### 5.1 アクセス管理
- 入社・異動・退職に伴うアカウント管理は Entra ID のプロビジョニング(SCIM)に連動させ、Snowflake 側の手動作成は行わない
- アクセス権申請はワークフロー経由とし、Spoke 管理者の承認後にプラットフォーム管理者が付与する
- 休眠アカウント(90 日間未ログイン)は月次棚卸しで無効化する

### 5.2 データ保護
- 機微情報(個人情報、未公開情報)は Dynamic Data Masking / Row Access Policy を適用する
- タグベースでの分類(CONFIDENTIAL / INTERNAL / PUBLIC)を実施し、マスキングポリシーをタグに紐づける
- DEV/STG 環境へのデータ複製時はマスク適用を必須とする

### 5.3 ネットワーク
- ネットワークポリシーで接続元 IP を社内・許可ベンダー拠点に限定する
- AWS 側は PrivateLink 経由の接続を基本とする

### 5.4 鍵・証明書管理
- サービスアカウントの Key Pair は 1 年ごとにローテーションする(Snowflake は 2 鍵登録可能なため無停止で切替)
- 秘密鍵は AWS Secrets Manager で管理し、ファイル配布は行わない

### 5.5 監査
- ACCESS_HISTORY / LOGIN_HISTORY / QUERY_HISTORY を監査証跡とし、保持期間(365 日)を超える分は S3 へ定期エクスポートして長期保管する
- 監査部門・監督当局からの照会に備え、抽出手順を手順書化しておく

---

## 6. 変更管理・リリース運用

### 6.1 変更の分類

| 分類 | 例 | 承認 | 適用方法 |
|---|---|---|---|
| 定常変更 | dbt モデル追加、ビュー変更 | PR レビュー(1 名以上) | CI/CD 自動適用 |
| 通常変更 | スキーマ変更、権限変更、WH 設定変更 | Spoke 管理者+プラットフォーム管理者 | schemachange / Terraform 経由 |
| 緊急変更 | 障害復旧のための即時修正 | 事後承認可(24h 以内に記録) | 手動可。実施ログ必須 |

### 6.2 ツールと責務分担

| レイヤ | ツール | 管理対象 |
|---|---|---|
| アカウント/インフラ | Terraform | アカウントパラメータ、WH、DB、ロール、ネットワークポリシー |
| スキーマ/DDL | schemachange | テーブル・ビュー等の DDL バージョン管理 |
| 変換ロジック | dbt | Silver/Gold 層のモデル、テスト、ドキュメント |

### 6.3 リリースフロー(標準)
1. 開発者が feature ブランチで開発(DEV 環境)
2. PR 作成 → CI(dbt build / lint / 単体テスト)
3. レビュー承認 → STG へ自動デプロイ → 結合検証
4. リリース承認(通常変更は変更管理票起票)
5. main マージ → PROD へデプロイ(schemachange / dbt 実行)
6. リリース後確認(スモークテスト、翌日のジョブ正常性確認)

### 6.4 ロールバック方針
- dbt モデル:直前バージョンの再デプロイで戻す
- DDL:schemachange のロールバックスクリプトを変更時に必ず用意する
- データ:Time Travel(標準 1 日、重要テーブルは 90 日)で復元。超過分は Fail-safe / バックアップから復旧

---

## 7. 障害対応

### 7.1 障害レベル定義

| レベル | 定義 | 目標復旧時間 | 例 |
|---|---|---|---|
| L1(重大) | 本番業務(NAV 算出・レポーティング等)に直接影響 | 4 時間 | 本番 MART 更新停止、Snowflake 障害 |
| L2(高) | 業務影響ありだが代替手段あり | 1 営業日 | Tableau 接続不可、一部ジョブ失敗 |
| L3(中) | 業務影響なし・限定的 | 3 営業日 | DEV 環境の不具合、性能劣化 |

### 7.2 対応フロー
1. 検知(監視アラート/ユーザー申告)
2. 一次切り分け(§7.4 参照)と障害レベル判定
3. L1 の場合:関係者招集、業務部門・情シスへ即時連絡
4. 復旧対応(必要に応じ緊急変更手順を適用)
5. 復旧確認・関係者への終報
6. 障害報告書作成(L1/L2 は必須)、再発防止策の起票

### 7.3 Snowflake 側障害の確認
- Snowflake Status ページで対象リージョンの稼働状況を確認する
- Snowflake 側起因の場合はサポートケースを起票し、社内へは影響範囲と代替手段を周知する

### 7.4 主要な切り分け観点(例)

| 事象 | 確認ポイント |
|---|---|
| ロード失敗 | COPY_HISTORY のエラー内容 → ファイル到着有無 → ステージ/外部連携(S3, SFTP)の疎通 |
| Tableau 接続エラー | OAuth トークン有効期限 → Entra ID 側のアプリ設定 → ネットワークポリシー → ロールの USAGE 権限 |
| クエリ遅延 | QUERY_PROFILE でスピル/スキャン量確認 → WH サイズ・同時実行 → クラスタリング状態 |
| 権限エラー | 現在のロール確認 → Future Grants 適用漏れ(スキーマレベル上書きの有無)→ GRANT 経路 |

---

## 8. データライフサイクル管理

### 8.1 保持期間

| レイヤ | 保持方針 | Time Travel |
|---|---|---|
| RAW | 原本は S3 に長期保管(法定保存期間に準拠)。Snowflake 上は 1 年 | 1 日 |
| STAGING | 直近処理分のみ(TRANSIENT テーブル) | 0〜1 日 |
| MART | 業務要件に応じ保持(基本 5 年以上) | 90 日(重要テーブル) |

### 8.2 アーカイブ・削除
- 保持期間超過データは月次バッチで S3(Glacier)へアーカイブ後、Snowflake から削除する
- 削除はデータオーナーの承認を得てから実施し、実施記録を残す

### 8.3 バックアップ・DR
- Time Travel + Fail-safe を基本とし、重要データはゼロコピークローンによる論理バックアップを週次取得する
- DR 要件が確定した場合はレプリケーション(別リージョン)を検討する(コスト影響を含め年次で再評価)

---

## 9. コスト管理

### 9.1 可視化
- WH・データベース・タグ(部門タグ)単位でクレジット消費を月次集計し、部門別レポートを作成する
- クエリタグ(QUERY_TAG)を dbt / ETL ジョブに必ず設定し、ジョブ単位の原価把握を可能にする

### 9.2 適正化の運用ルール
- ウェアハウスは AUTO_SUSPEND 60 秒を標準とし、常時起動は禁止する
- アドホック分析用と定常バッチ用の WH を分離し、サイズ変更はプラットフォーム管理者の承認制とする
- 月次で「高コストクエリ Top 10」をレビューし、改善(マテリアライズ、クラスタリング、WH 見直し)を実施する
- 未使用テーブル・古いクローンの棚卸しを月次で行い、ストレージコストを抑制する

---

## 10. 定常運用スケジュール

| タイミング | 作業 | 担当 |
|---|---|---|
| 日次(朝) | 夜間ジョブ実行結果確認、失敗時の一次対応 | 運用担当 |
| 日次 | クレジット消費確認 | 運用担当 |
| 週次 | 権限変更・セキュリティログレビュー | 運用担当・情シス |
| 月次 | コストレポート、休眠ユーザー・未使用オブジェクト棚卸し | 運用担当 |
| 四半期 | RBAC 棚卸し、Resource Monitor 予算見直し | 基盤オーナー |
| 年次 | 鍵ローテーション、DR 方針・保持期間の見直し、運用設計書改訂 | 基盤オーナー |

---

## 11. 申請・問い合わせ窓口

| 種別 | 窓口 | SLA(目安) |
|---|---|---|
| アクセス権申請 | ワークフローシステム → Spoke 管理者 | 3 営業日 |
| 新規 Spoke / DB 作成 | データ活用推進 | 5 営業日 |
| 障害連絡 | 運用担当(Teams チャネル/内線) | 即時 |
| コスト・利用相談 | データ活用推進 | 随時 |

---

## 12. 改訂履歴

| 版数 | 日付 | 改訂内容 | 作成者 |
|---|---|---|---|
| 1.0 | 2026-07-10 | 初版作成 | データ活用推進 |

---

## 付録A. 用語集(抜粋)

| 用語 | 説明 |
|---|---|
| Hub & Spoke | 共通データ・統制を Hub に集約し、部門利用を Spoke に分離する構成 |
| メダリオン | RAW → STAGING → MART の段階的データ精製アーキテクチャ |
| Future Grants | 将来作成されるオブジェクトへの権限の事前付与 |
| Time Travel | 過去時点のデータへ遡って参照・復元できる Snowflake 機能 |
| Resource Monitor | クレジット消費に閾値を設けて通知・停止する機能 |
